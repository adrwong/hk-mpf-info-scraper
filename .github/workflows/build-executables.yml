name: Build Executables

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: mpf-scraper-linux
            asset_name: mpf-scraper-linux
          - os: windows-latest
            artifact_name: mpf-scraper-windows.exe
            asset_name: mpf-scraper-windows.exe

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller pandas requests beautifulsoup4 lxml

    - name: Build executable with PyInstaller
      run: |
        pyinstaller --onefile --name ${{ matrix.artifact_name }} mpf_scrape_json.py

    - name: Test executable (Linux)
      if: runner.os == 'Linux'
      run: |
        chmod +x dist/${{ matrix.artifact_name }}
        echo "Executable created successfully"
        file dist/${{ matrix.artifact_name }}

    - name: Test executable (Windows)
      if: runner.os == 'Windows'
      run: |
        echo "Executable created successfully"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: dist/${{ matrix.artifact_name }}
        if-no-files-found: error

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/${{ matrix.artifact_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
